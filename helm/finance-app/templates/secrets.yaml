# Example secret - DO NOT commit real secrets
# Use sealed-secrets, external-secrets, or similar in production
apiVersion: v1
kind: Secret
metadata:
  name: finance-app-secrets
  labels:
    {{- include "finance-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres:5432/finance"
  jwt-secret: "CHANGE_ME_IN_PRODUCTION"
  google-client-id: "CHANGE_ME"
  google-client-secret: "CHANGE_ME"
