# Example environment variables for local/cloud development
# Copy to .env and fill in as needed (do not commit secrets)

# API URL (use service name inside Docker; switch to http://localhost:4000 when running backend on host)
VITE_API_URL="http://localhost:4000"

# Google OAuth Client ID (same used by backend)
VITE_GOOGLE_CLIENT_ID=""
